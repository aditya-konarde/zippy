import Foundation
import Network

// Exports all public types and interfaces for the Networking module
public typealias NetworkMonitorType = NetworkMonitor
public typealias NetworkMonitorDelegateType = NetworkMonitorDelegate
public typealias ConnectionManagerType = ConnectionManager
public typealias ConnectionManagerDelegateType = ConnectionManagerDelegate
public typealias NetworkManagerType = NetworkManager

public class NetworkingModule {
    public static func initialize() {
        // Initialize any module-level setup here
    }
} 